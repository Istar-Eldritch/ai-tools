description = "Minimal profile for basic Claude Code usage"

rw_paths = [
    "~/.claude/",
]

ro_paths = [
    "/usr",
    "/lib",
    "/lib64",
    "/bin",
    "/sbin",
    "/etc/resolv.conf",
    "/etc/hosts",
    "/etc/ssl",
    "/etc/alternatives/",
    "~/code/ai_tools",
    "~/.local",
    "~/.config",
    "~/.gitconfig",
    "~/.gitignore_global"
]

excluded_paths = []

env = []
